# 性能测试指南

## 🚀 快速测试（30分钟）

测试当前配置的性能瓶颈：

```bash
sbatch test_performance.slurm
```

查看结果：
```bash
tail -f logs/perf_test_*.out
```

## 📊 性能报告解读

测试会每20个batch输出一次性能分析，例如：

```
======================================================================
⏱️  性能分析 [Batch 20/1670]
======================================================================
  组件          时间/batch      占比   每秒batches
----------------------------------------------------------------------
  数据加载        2500.0ms    65.0%        8.00
  GPU传输           45.0ms     1.2%      444.44
  Forward          600.0ms    15.6%       33.33
  Backward         500.0ms    13.0%       40.00
  Optimizer        200.0ms     5.2%      100.00
----------------------------------------------------------------------
  总计            3845.0ms/batch    整体速度: 5.20 batches/sec

  ⚠️  提示: 数据加载占比65% - CPU有瓶颈
======================================================================
```

### 判断标准：

- ✅ **数据加载 < 50%** → 训练效率良好
- ⚠️ **数据加载 50-70%** → 有CPU瓶颈，但可接受
- 🔴 **数据加载 > 70%** → 严重瓶颈，需要优化

## 🔧 根据结果优化

### 如果数据加载 > 70%：

1. **降低batch size**
   ```bash
   # 编辑配置文件
   vim configs/vitscope_gpu12.yaml
   # 将 bs: 768 改为 bs: 512 或 bs: 384
   ```

2. **联系管理员**
   - 申请增加 num_workers 到 8-12
   - 或请求复制数据到 gpu12 本地 SSD

### 如果数据加载 < 50%：

✅ 可以尝试增加 batch size 以更好利用GPU！

```bash
# 测试更大的batch size
vim configs/vitscope_gpu12.yaml
# 将 bs: 768 改为 bs: 1024
```

## 📝 完整训练配置

测试满意后，使用完整训练：

```bash
# 修改测试配置为100 epochs
vim configs/vitscope_gpu12.yaml
# n_epochs: 1 → n_epochs: 100

# 提交正式训练
sbatch run_vitscope_final.slurm
```



