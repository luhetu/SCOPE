task: det
model: vit
data_dir: /home3/dnrx52/SCOPE/datasets/coco
bs: 2
pretrained: ./checkpoint/pretrained/vit_cls_size224_patch16_dim192_depth12_best.pth  # 72.11%
det_neck_type: fpn  # Paper-style Mask R-CNN + FPN
img_scale: [1333, 800]
size: 224
patch: 16
dim: 192
depth: 12
heads: 3
mlp_dim: 768
dim_head: 64

n_epochs: 12  # 1x schedule
lr: 1.25e-5  # 1e-4 * (bs/16) with bs=2
min_lr: 0.000001
warmup_iters: 1000
weight_decay: 0.05
opt: adamw
betas: [0.9, 0.999]
amp: true
nowandb: false
